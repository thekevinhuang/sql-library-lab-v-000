CREATE TABLE * (
  id INTEGER PRIMARY KEY,
  
);
CREATE TABLE * (
  id INTEGER PRIMARY KEY,
  
);
CREATE TABLE * (
  id INTEGER PRIMARY KEY,
  
);
CREATE TABLE * (
  id INTEGER PRIMARY KEY,
  
);
CREATE TABLE * (
  id INTEGER PRIMARY KEY,
  
);